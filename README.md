# Page with coffee list

## Available Scripts

To install all dependencies run

### `npm install`

To run the project locally, you can run:

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
